[package]
name = "shared-bigint"
version = "0.1.0"
edition = "2024"
description = "Multi-precision helpers: batched field inversion, mp utilities"

[dependencies]
shared-field = { path = "../field" }
crypto-bigint = { version = "0.5", features = ["rand_core"] }
num-bigint = "0.4"
num-integer = "0.1"
num-traits = "0.2"
