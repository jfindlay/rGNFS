//! CLI binary `rho-factor`.
//!
//! Populated in Phase 2. Usage: `rho-factor <N> [--threads N] [--batch-size B]`.

fn main() {
    eprintln!("rho-factor: not yet implemented (Phase 2)");
    std::process::exit(1);
}
